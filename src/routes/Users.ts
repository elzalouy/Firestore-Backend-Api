import { Router } from "express";

const router = Router();
router.get("getUsers", () => {});
router.post("addUser", () => {});
router.delete("deleteUser", () => {});
router.put("updateUser", () => {});
export default router;
